import React from 'react'

const AppointmentInfoPage = async ({params}) => {
    const {id} = await params
    const appointId = id
  return (
    <div>
        AppointmentInfoPage</div>
  )
}

export default AppointmentInfoPage